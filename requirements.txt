# Requirements for Maze Myth Dynamic Honeypot
# Install with: pip install -r requirements.txt
#
# ✅ Compatible with Python 3.9, 3.10, 3.11, 3.12, 3.13, 3.14
# ✅ Works on Windows, macOS, Linux
# ✅ No compilation from source required

# ── Web framework ──────────────────────────────────────────
flask>=3.0.0,<4.0.0
flask-cors>=4.0.0,<6.0.0

# ── Fake banking data generation ───────────────────────────
faker>=20.0.0

# ── Bait file generation ───────────────────────────────────
reportlab>=4.0.7
openpyxl>=3.1.2
fpdf2>=2.7.6

# Pillow: installed only if a compatible prebuilt wheel exists.
# On Python 3.14 you may need: pip install pillow --pre
# The honeypot runs fine without it (PDF/Excel generation
# falls back to text-only mode automatically).
pillow>=10.4.0; python_version < "3.14"

# ── LLM — Google Gemini ────────────────────────────────────
google-generativeai>=0.8.3

# ── Config / utilities ─────────────────────────────────────
python-dotenv>=1.0.0

# ── State ──────────────────────────────────────────────────
# SQLite3 is part of Python's standard library — no package needed

# ── Production WSGI server (used by Docker / Gunicorn) ─────
# gunicorn does not support Windows natively — skip on Windows
gunicorn>=22.0.0; sys_platform != "win32"

# ── RAG / Data Processing ──────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
